name: Build and Package App

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      # -----------------------------------------------------------
      # DEPENDENCIES
      # -----------------------------------------------------------
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # -----------------------------------------------------------
      # BUILD (Unified PyInstaller for all OS)
      # -----------------------------------------------------------
      - name: Build with PyInstaller
        run: |
          pip install pyinstaller
          pyinstaller --onefile --windowed src/quickDrawQT.py --name "Quick Draw"

      # -----------------------------------------------------------
      # PACKAGING (Creating the Zips)
      # -----------------------------------------------------------
      - name: Zip Windows Release
        if: runner.os == 'Windows'
        run: Compress-Archive -Path "dist\Quick Draw.exe" -DestinationPath QuickDraw-Windows.zip

      - name: Zip Linux Release
        if: runner.os == 'Linux'
        run: cd dist && zip -r ../QuickDraw-Linux.zip "Quick Draw"

      - name: Zip macOS Release
        if: runner.os == 'macOS'
        run: cd dist && zip -r ../QuickDraw-MacOS.zip "Quick Draw.app"

      # -----------------------------------------------------------
      # UPLOAD (Grabbing the Zips)
      # -----------------------------------------------------------
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: QuickDraw-${{ runner.os }}
          path: QuickDraw-*.zip